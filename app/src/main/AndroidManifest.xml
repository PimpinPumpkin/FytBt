<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="31" xmlns:tools="http://schemas.android.com/tools" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />

    <!-- Legacy permissions kept for completeness on pre-API-31 stacks; harmless on API 33. -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />

    <!-- Dialer: phone contacts + call history come from the phone via Bluetooth PBAP, synced into
         the standard providers. CALL_PHONE places a call (handed to the system/stock app over HFP). -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <!-- Online album-art fallback (iTunes Search) for sources that don't send art over Bluetooth
         (Spotify, iPhones). Only used when the phone supplies no AVRCP cover art. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature android:name="android.hardware.bluetooth" android:required="true" />

    <application
        android:label="@string/app_name"
        android:icon="@android:drawable/stat_sys_data_bluetooth"
        android:allowBackup="false"
        android:supportsRtl="true"
        android:theme="@style/Theme.FytBt">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            Notification listener stub. Doesn't process any notifications, but its mere presence
            (once the user grants Notification Access) makes MediaSessionManager.getActiveSessions
            available so we can read the AVRCP-backed MediaSession from com.android.bluetooth.
        -->
        <service
            android:name=".media.FytNotificationListener"
            android:label="@string/notification_listener_label"
            android:exported="false"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
    </application>
</manifest>
